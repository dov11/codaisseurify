<h1>Songs by <%=@artist.name%></h1>
<%= image_tag @artist.pictures.first.image_url.thumbnail %>
<%@artist.songs.each do |song| %>
<p><%= link_to song.name, artist_song_path(@artist, song)%>
  <%end%>
